import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.travelfalltech.guitartool',
  appName: 'Guitar Tool',
  webDir: 'dist'
};

export default config;
